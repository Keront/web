git config --global user.name "Keront"
git config --global user.email "akill22077@gmail.com"

git config --global http.sslBackend schannel

#Настройки > Terminal > Integrated > Env: Windows
{
    "files.autoSave": "afterDelay",
    "terminal.integrated.env.windows": {
        

        "PATH": "C:\\Users\\student\\Downloads\\node-v25.9.0-win-x64;${env:PATH}"
    }
}